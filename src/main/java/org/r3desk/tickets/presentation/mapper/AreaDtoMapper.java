package org.r3desk.tickets.presentation.mapper;

import org.r3desk.tickets.domain.model.Area;

public class AreaDtoMapper {
/*
    public static Area toDomain(AreaReq dto) {
        var domain = new Area();
        domain.setId(dto.getId());
        domain.setNombre(dto.getNombre());
        return domain;
    }

    public static AreaReq toEntity(Area domain) {
        var dto = new AreaReq();
        dto.setId(domain.getId());
        dto.setNombre(domain.getNombre());
        return dto;
    }

 */
}
