package org.r3desk.tickets.presentation.mapper;

import org.r3desk.tickets.domain.enums.Priority;
import org.r3desk.tickets.domain.model.Ticket;

public class TIcketDtoMapper {
/*
    public static Ticket toDomain(CreateTicketRequest request) {
        var domain = new Ticket();
        domain.setId(request.getId());
        domain.setSourceGroup(GrupoDtoMapper.toDomain(request.getSourceGroupReq()));
        domain.setTargetGroup(GrupoDtoMapper.toDomain(request.getTargetGroupReq()));
        domain.setSourceArea(AreaDtoMapper.toDomain(request.getSourceAreaReq()));
        domain.setTargetArea(AreaDtoMapper.toDomain(request.getTargetAreaReq()));
        domain.setTitle(request.getTitle());
        domain.setDescription(request.getDescription());
        domain.setStatus("new");
        domain.setPriority(Priority.valueOf(request.getPriority().name()));

        return domain;
    }

 */
}
