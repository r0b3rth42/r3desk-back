package org.r3desk.tickets.presentation.mapper;

import org.r3desk.tickets.domain.model.Grupo;
import org.r3desk.tickets.infrastructure.adapter.out.entity.GrupoEntity;

public class GrupoDtoMapper {
/*
    public static Grupo toDomain(GroupReq req) {
        var domain = new Grupo();
        domain.setId(req.getId());
        domain.setNombre(req.getNombre());
        return domain;
    }

    public static GroupReq toEntity(Grupo domain) {
        var dto = new GroupReq();
        dto.setId(domain.getId());
        dto.setNombre(domain.getNombre());
        return dto;
    }

 */
}
